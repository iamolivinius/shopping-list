import {
  formatPrice
} from 'shopping-list/helpers/format-price';

module('FormatPriceHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatPrice(42);
  ok(result);
});
